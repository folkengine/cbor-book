# Matching Patterns with `envelope` CLI

```admonish wip "A much nicer tutorial is coming."
For now this is a redirect the the `envelope` [CLI documentation at GitHub](https://github.com/BlockchainCommons/bc-envelope-cli-rust/tree/master/docs), which includes an introduction to [Pattern Matching](https://github.com/BlockchainCommons/bc-envelope-cli-rust/blob/master/docs/PatternMatching.md).

The appendices include an [Envelope Patex Reference](../appendices/envelope_patex.md).
```
